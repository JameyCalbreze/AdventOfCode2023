use crate::utils;

/**
 * This problem will help learn about Rust traits.
 * This will be as simple as calling sort on an array of hands.
 * We implement the comparison methods, sort, and then iterate over
 * all the cards to get the solution
 */
pub fn get_solution() {
    let input = utils::prep_input("input/problem_07.txt");
    part_one(&input);
    part_two(&input);
}

fn part_one(input: &Vec<String>) {

}

fn part_two(input: &Vec<String>) {
    
}
